# 02 模板：开船日、逐行有效期和中文状态

2026-09-15，依据用户提供的 02 模板截图。

- 保留当前三条航线、九个箱型价格；样例开船日分别为「每周五」「每月15日」「2026年9月15日」。同步更新 `excel-import-fixtures/02_chinese_headers_global_validity.xlsx`。
- 「有效开始日期」「有效结束日期」自动映射至生效日和失效日。逐行填写优先，留空沿用表头统一有效期；非法逐行日期不能被表头默认值掩盖。
- 「状态（必填）」可识别括号中的必填标记，支持草稿、启用、已过期、停用以及 DRAFT、ACTIVE、EXPIRED、INACTIVE。有状态列时空值报行级错误；旧模板无状态列时仍默认为草稿。
- 开船日列同时接受中文年月日、`yyyy-mm-dd`、`yyyy/m/d`、`yyyy.m.d` 和 Excel 日期单元格。具体日期写入 ETD；纯日期按 UTC 零点保存，不受服务器本地时区影响。
- 每周和每月船期保留原文，不推算下一班日期。沿用现有 `Schedule:` 备注保存方式，随箱型价格落库，无数据库迁移；预览通过 `sailingPattern` 显示周期船期。
- 导入预览展示开船日与中文状态。不认识的船期或无效开船日期给出核对提醒并保留原文；无效状态和有效期阻止确认导入。
- 回归覆盖三种开船日的解析与落库、日期格式、非法日期、中文状态、必填检查及行级有效期覆盖。
- 后台运价列表在「箱型 / 成本」与「有效期」之间展示「开船日」：具体 ETD 优先，否则读取已导入的 `Schedule:` 备注并去重，无数据时显示「—」。有效期直接显示数据库保存的 Excel 逐行日期，不从船期推算；修改模板不会追溯修改已存在的运价。
- 客户运价桌面列表、手机卡片及获取正式报价弹窗统一展示「开船日」和完整有效期；客户管理员与普通客户用户使用同一显示逻辑。客户 API 仅从备注中提取受支持的周期船期，不返回内部备注、成本和供应商。内部用户共用后台列表，角色权限及租户隔离不变。
