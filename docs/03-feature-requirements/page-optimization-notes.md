# 逐页优化记录

## 2026-09-12 — 销售的正式报价待办未展示

- 状态：已记录，待核实；沿用本轮逐项记录需求的方式，暂不修改代码。
- 用户反馈：销售的正式报价待办未展示。
- 截图现状：客户门户首页「待处理事项」中已显示 `QT202609000003 待确认` 和「确认报价」入口；「未读通知」区域只见两条运输通知。截图未展示销售端工作台，不能据此确认缺失环节。
- 待排查：明确是销售端待处理正式报价任务缺失，还是销售发布正式报价后客户侧通知缺失；核对账号角色、报价状态、待办筛选与数量统计、发布事件及通知生成情况。
- 后续验收方向：对应角色在正确的业务阶段能看到应处理的正式报价及详情入口；待办数量与明细一致，已处理事项随状态更新，并保留租户及客户公司隔离。

## 2026-09-12 — 正式报价发布前考虑增加管理员审核

- 状态：已记录，待确认流程；沿用本轮逐项记录需求的方式，暂不修改代码。
- 页面：运营后台「发布 Formal Quote」确认弹窗，对应向客户发布正式报价的阶段。
- 用户反馈：这个阶段可能还需要管理员审核。
- 待确认方向：销售完成报价后提交管理员审核，审核通过后再允许向客户发布正式报价；目前为候选流程，尚未确定所有报价都必须审核。
- 待明确：审核人是否为租户管理员、审核触发条件、审核通过后由谁发布、驳回后的修改与重提方式，以及已审核报价再次改价是否需要重新审核。
- 实施约束：如确认引入，审核及发布资格须由服务端校验，保留租户隔离、审核记录及操作审计；状态设计需结合现有报价状态机确定，不提前引入通用审批引擎。

## 2026-09-12 — 利润概览展示全部新增额外费用

- 状态：已记录，待实施；沿用本轮逐项记录需求的方式，暂不修改代码。
- 页面：运营后台报价详情的「利润概览」区域。
- 用户要求：在此展示所有新增的额外费用。
- 展示方向：在概览区域逐项列出新增额外费用，包含费用名称、币种、计费单位及数量、成本和对客金额，便于核对汇总组成。
- 汇总要求：额外费用的成本及对客金额纳入对应币种的总成本、报价总额、预计毛利和毛利率计算；与基础费用一并核对，避免遗漏或重复计入，不跨币种直接相加。
- 待确认费用及缺失成本需明确标记，不按零成本处理为完整毛利；成本和利润仍仅向有权限的内部人员展示。
- 关联需求：与「调整报价时结构化费用并计算毛利」使用一致的费用明细数据；本次仅记录展示及汇总需求，未核实当前实现。

## 2026-09-12 — 调整报价时结构化费用并计算毛利

- 状态：已记录，待细化及实施；沿用本轮逐项记录需求的方式，暂不修改代码。
- 页面：运营后台「调整报价价格」弹窗，用户标注的内部改价原因附近区域。
- 用户要求：将字段结构化，用于计算毛利。
- 建议字段：按费用项记录费用名称、计费单位、数量、币种、采购单价及销售单价，展示成本小计、销售小计和毛利；具体字段布局实施时再确定。
- 计算方向：同币种下，成本小计 = 采购单价 × 计费数量，销售小计 = 销售单价 × 计费数量，毛利 = 销售小计 − 成本小计。按费用项实际计费单位取数量，不将所有费用都按箱量计算；不同币种分别汇总，不隐式换汇相加。
- 数据完整性：成本缺失或费用待确认时明确标记，不能将缺失成本当作零而展示完整毛利；改价原因保留为独立说明，不从自由文本推断金额。
- 实施约束：服务端使用 Decimal 校验及计算，保存价格变更审计；成本和毛利仅向有权限的内部人员展示，不泄露到客户页面、报价说明或客户 PDF。
- 待实施时明确：可编辑的成本范围、费用项增删权限、是否展示毛利率，以及与现有报价费用明细和「改价后生成新运价」的关联规则。

## 2026-09-12 — 报价改价后可生成新运价

- 状态：已记录，待实施；沿用本轮逐项记录需求的方式，暂不修改代码。
- 页面：运营后台报价详情，关联截图中的「运价来源与发送记录」区域。
- 用户要求：如果修改了价格，允许生成一条新的运价。
- 交互方向：报价改价后提供可选的「生成新运价」操作，由用户主动选择，不在每次改价时自动创建。
- 实施约束：新运价使用独立编号，保留原运价及历史报价快照；创建时校验运价管理权限、租户归属及必填数据，并记录来源和操作审计。
- 待实施时明确：修改采购成本或客户售价时分别如何映射到新运价；航线、船司、箱型、附加费和有效期的继承及核对范围；新运价状态、适用客户范围及当前报价是否重新关联。不得将客户专属报价默认推广为所有客户可用的运价。

## 2026-09-12 — 报价详情中的客户名称链接到公司详情

- 状态：已记录，待实施；沿用本轮逐项记录需求的方式，暂不修改代码。
- 页面：运营后台报价详情顶部摘要的「客户」字段，截图中的 Northstar Trading Co., Ltd.。
- 用户要求：客户公司名称增加链接，点击进入对应客户公司详情。
- 验收要点：链接使用当前报价关联的客户公司 ID，进入对应的后台客户详情页；名称应呈现可点击样式并支持键盘访问。
- 实施约束：沿用现有客户查看权限及租户隔离校验，不因增加链接扩大数据访问范围。

## 2026-09-12 — 报价审核信息增加拟参加船期

- 状态：已记录，待实施；沿用本轮逐项记录需求的方式，暂不修改代码。
- 页面：运营后台报价详情的「报价审核信息」区域，报价有效期右侧的截图红框位置。
- 用户要求：增加「参加几号的船期」字段，暂记为「拟参加船期」，用于填写本次报价计划参加哪一天的船期。
- 后续验收方向：支持选择具体日期，随「保存审核信息」保存并正确回显；与报价有效期、工厂预计装货日期分别表达，不将计划船期视为已确认订舱。
- 待实施时确认：是否必填、与来源运价 ETD 的关系，以及客户报价页面、PDF 和后续订舱中的展示或沿用范围；本次不预先扩大要求。

## 2026-09-12 — 正式报价申请增加工厂预计装货日期

- 状态：已记录，待实施；沿用本轮逐项记录需求的方式，暂不修改代码。
- 页面：客户门户「获取正式报价」弹窗，箱量右侧、货物概况上方的截图红框位置。
- 用户要求：在该位置增加「工厂预计装货日期」字段。
- 后续验收方向：客户可选择工厂预计装货的日期，并随报价申请保存；该日期与船舶预计离港日（ETD）分别表达，不混用。
- 待实施时确认：是否必填、允许的日期范围及后续报价/订舱页面的展示范围；本次用户未指定这些规则，不预先扩大要求。

## 2026-09-12 — 正式报价申请的提货地点、派送地点必填

- 状态：已记录，待实施；按用户要求暂不修改代码。
- 页面：客户门户「获取正式报价」弹窗，截图红框中的「提货地点」「派送地点」。
- 用户要求：这两个地点字段为必填。
- 截图现状：两个字段已有必填标记，且附加服务均已勾选；尚未核实实际提交校验。
- 后续验收方向：两个地点提供明确的必填提示，缺失或仅填空白时不能提交，并在对应字段显示错误；前后端校验保持一致。
- 待实施时核对：未勾选对应附加服务时的字段展示及必填规则。截图未覆盖该情形，本次不据此推定条件规则；也不将截图箭头解读为另行要求调整字段位置。

## 2026-09-12 — 运价列表增加开船日

- 状态：已记录，待实施；按用户要求暂不修改代码。
- 页面：运营后台 `/admin/rates` 运价列表。
- 用户要求：列表加上「开船日」，无需打开编辑抽屉即可查看。
- 截图现状：列表展示运价编号、航线、船司、箱型/成本、有效期、供应方/合约、状态及操作，未展示开船日。
- 验收要点：列表提供清晰的「开船日」展示，与运价有效期区分；具体 ETD 日期与周期性船期按实际数据表达，缺失时明确显示为空，不推算或编造日期。
- 关联问题：与下方「运价导入后开船日未展示」一并核对数据来源及展示一致性；本次仅补充列表展示需求，未开展排查或实现。

## 2026-09-11 — 运价导入后开船日未展示

- 状态：已记录，待排查；按用户要求暂不修改代码。
- 页面：运营后台 `/admin/rates` →「编辑运价」抽屉；截图显示 Excel 导入完成后打开运价。
- 用户反馈：开船日没有展示出来。
- 截图示例：运价 `RATE202609000005`，厦门（CNXMN）→ 雅加达（IDJKT），船司 CNC，航线服务 JKT1；ETD 输入框为空，箱型备注中可见截断的 `Schedule: 每…` 文本。
- 待排查：核对原 Excel 开船日内容、导入解析与保存结果、接口返回和编辑表单回填，确认原值是具体日期还是每周开船规律；目前尚未确认原因。
- 后续验收方向：导入的开船日信息应在运价页面清晰展示；区分具体 ETD 日期与周期性船期，不能将每周开船规律误当作具体日期。

## 2026-09-09 — 删除未使用的运价

- 状态：已修改代码；按用户要求未运行测试。
- 运价列表增加红色「删除」按钮，点击后确认运价编号、航线和不可恢复的影响。
- 未被任何报价引用的运价允许删除，箱型价格和附加费随主记录一并删除。
- 已被报价引用的运价禁用删除入口，显示「已用于报价，只能停用」；不按报价状态放宽删除。
- 后端新增 `DELETE /api/v1/rates/:id`，要求 `rate.manage`，检查租户归属，并在事务中锁定运价后检查引用，避免与创建报价发生竞态。
- 删除与 `RATE_DELETED` 审计记录在同一事务完成；已引用返回 `RATE_IN_USE`，不存在或跨租户返回 `RATE_NOT_FOUND`。
- 复用现有外键级联规则，无数据库迁移；不会主动清理现有运价，需用户逐条确认。

## 2026-09-09 — 运价 Excel 导入减轻操作负担

- 状态：已修改前端代码；按用户要求未运行测试。
- 用户反馈：智能导入界面太重，用户可能不会使用。
- 调整：首次只展示选择文件、拖放区域和辅助模板链接；读取完成后切换到运价核对，导入后仅显示结果。
- 核对重点：航线、船司、有效期、采购成本、标准售价和附加费；去掉识别评分、重复摘要和步骤栏，导入按钮按运价条数表达。
- 渐进展示：工作表、列标题与列对应关系收在「识别不对？调整工作表和列」；问题集中展示，提醒可展开核对；统一日期和币种填写完后点击应用，避免每次输入就发起预览。
- 前台取消「旧版导入方式」分支，保留已有后端接口；正常入口统一经分析、预览和确认后导入。保留格式记忆配置、阻断错误及提醒确认。
- 无数据库变更；本次仅调整前端交互，不新增解析能力。

## 2026-09-09 — 登录页租户代码

- 状态：已记录，待实施；本次不修改页面代码。
- 页面：用户截图中的运营后台「员工登录」页。
- 用户要求：真实客户使用时，租户代码不能让客户填写，也不能编辑。
- 验收要点：真实客户登录时无需手动输入租户代码；页面不提供可编辑的租户代码输入框。
- 待实施时确定：该员工登录入口如何取得租户上下文。客户门户已有品牌化入口方案，参见 `../01-product/customer-portal-white-label-optimization.md`；本记录不预先决定员工入口采用相同方案。

## 2026-09-09 — 新建客户自动生成客户代码

- 状态：已记录，待实施；本次不修改页面代码。
- 页面：运营后台「客户」→「新建客户」抽屉。
- 用户反馈：用户不知道客户代码应如何填写，希望由系统生成。
- 优化要求：新建客户不再要求用户手动填写客户代码，由后端在保存客户时自动生成。
- 建议交互：创建表单移除客户代码必填输入框；保存后在列表或详情中展示生成的代码。
- 实施约束：代码在租户内唯一，生成过程须支持并发；保留已有客户代码。具体编码格式待实施时结合批准的设计文档和现有实现确定。

## 2026-09-09 — 客户详情页标签与按钮颜色

- 状态：已记录，待实施；本次不修改页面代码。
- 页面：运营后台「客户详情」，截图红框中的联系人角色标签和客户账号区域「开通账号」按钮。
- 用户要求：增加颜色，让这些元素更醒目。
- 优化要求：为「主要联系人」「订舱联系人」「单证联系人」标签增加有区分度的颜色；增强「开通账号」按钮的背景色和文字对比度。
- 实施要点：同类联系人标签颜色保持一致，保留文字含义；标签与可点击按钮保持视觉区分，文字需清晰可读。具体配色在实施时结合现有主题确定。

## 2026-09-09 — 创建与编辑客户的税号字段不一致

- 状态：已核实并记录，待实施；本次不修改页面代码。
- 用户反馈：创建客户时没有税号，编辑客户时却出现税号。
- 代码核实：新建页 `apps/web/app/admin/customers/page.tsx` 没有税号输入及提交字段；详情页的编辑表单包含 `taxId`；后端创建 DTO 和服务已支持可选税号。因此当前差异来自前端表单字段不一致。
- 建议修正：新建客户补充可选「税号」字段并提交给现有接口，与编辑表单保持一致；允许创建时留空、后续补充，避免用户为填写税号必须保存后再次编辑。

## 2026-09-09 — 开通客户账号入口去重与当前页办理

- 状态：已核实并记录，待实施；本次不修改页面代码。
- 用户反馈：顶部「开通客户账号」与客户账号区域「开通账号」重复，且开通时跳到用户管理页打断当前操作。
- 代码核实：两个入口均链接到 `/admin/users?customerCompanyId=${customerId}&createCustomer=1`，当前通过用户管理页承接账号创建。
- 优化要求：去掉重复入口，并支持在客户详情当前页完成账号开通。
- 建议交互：删除页面顶部的入口，保留「客户账号」区域按钮并统一为「开通客户账号」；点击打开当前页抽屉，自动关联当前客户公司；创建成功后关闭抽屉并刷新账号列表和数量。
- 实施约束：复用现有账号创建能力及校验，保留服务端权限、租户隔离及客户公司归属检查；提交失败时在抽屉内显示错误并保留输入。
